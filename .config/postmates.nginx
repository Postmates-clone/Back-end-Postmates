server {
    listen 80;

    server_name default_server;
    charset utf-8;

    location / {
        include         /etc/nginx/proxy_params;
        proxy_pass      http://unix:/run/postmates.sock;
    }

    location /static/ {
        alias           /srv/postmates/staticfiles/;
    }
}
